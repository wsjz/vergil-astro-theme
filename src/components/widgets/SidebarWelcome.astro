---
import siteConfig from '../../data/site-config';

const text = siteConfig.sidebar?.components?.welcome?.text ?? '欢迎来到我的博客';
---

<div class="px-4 py-4">
    <div class="rounded-lg bg-accent/5 border border-accent/10 px-3 py-2.5">
        <p class="text-xs text-secondary leading-relaxed">{text}</p>
    </div>
</div>
