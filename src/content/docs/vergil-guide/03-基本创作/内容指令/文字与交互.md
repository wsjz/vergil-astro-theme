---
title: 文字与交互（14个指令）
order: 5
---

# 文字与交互指令

用于美化文字和增加互动性。

---

## 高亮（mark）

::::tabs
tab: 演示效果

- 默认主题色高亮：这是一段 :mark[关键内容] 被高亮显示
- 黄色高亮：:mark[需要特别注意]{color="yellow"}
- 红色高亮：:mark[重要警告]{color="red"}
- 绿色高亮：:mark[成功提示]{color="green"}
- 蓝色高亮：:mark[信息说明]{color="blue"}
- 紫色高亮：:mark[特色标注]{color="purple"}

tab: 示例代码

`````markdown
- 默认主题色高亮：这是一段 :mark[关键内容] 被高亮显示
- 黄色高亮：:mark[需要特别注意]{color="yellow"}
- 红色高亮：:mark[重要警告]{color="red"}
- 绿色高亮：:mark[成功提示]{color="green"}
- 蓝色高亮：:mark[信息说明]{color="blue"}
- 紫色高亮：:mark[特色标注]{color="purple"}
`````

- `color` 可选值：`yellow`、`green`、`red`、`blue`、`purple`、`accent` 或任意十六进制色值
- 不传 `color` 时默认使用主题色

::::

---

## 下划线（u）

::::tabs
tab: 演示效果

- 默认下划线：这是一段 :u[带下划线] 的文字
- 蓝色下划线：:u[蓝色标注]{color="blue"}
- 红色下划线：:u[红色警示]{color="red"}
- 绿色下划线：:u[绿色强调]{color="green"}

tab: 示例代码

`````markdown
- 默认下划线：这是一段 :u[带下划线] 的文字
- 蓝色下划线：:u[蓝色标注]{color="blue"}
- 红色下划线：:u[红色警示]{color="red"}
- 绿色下划线：:u[绿色强调]{color="green"}
`````

- `color` 可选值：`blue`、`green`、`red`、`yellow`、`purple` 或任意色值
- 不传 `color` 时默认使用主题色

::::

---

## 着重号（emp）

::::tabs
tab: 演示效果

中文传统排版中的着重号，用于 :emp[需要特别强调] 的内容，常见于 :emp[法律条文]、:emp[合同条款] 等重要文本中。

tab: 示例代码

`````markdown
中文传统排版中的着重号，用于 :emp[需要特别强调] 的内容，常见于 :emp[法律条文]、:emp[合同条款] 等重要文本中。
`````

- 使用点状着重号，模拟中文排版传统
- 不支持 `color` 属性，使用默认文字颜色

::::

---

## 波浪线（wavy）

::::tabs
tab: 演示效果

- 默认波浪线：这是一个 :wavy[波浪线标注] 的示例
- 红色波浪线：:wavy[拼写错误提示]{color="red"}
- 蓝色波浪线：:wavy[语法建议]{color="blue"}

tab: 示例代码

`````markdown
- 默认波浪线：这是一个 :wavy[波浪线标注] 的示例
- 红色波浪线：:wavy[拼写错误提示]{color="red"}
- 蓝色波浪线：:wavy[语法建议]{color="blue"}
`````

- `color` 可选值：`blue`、`green`、`red`、`yellow`、`purple` 或任意色值
- 不传 `color` 时默认使用主题色
- 常用于标注拼写错误、语法提示等场景

::::

---

## 删除线（del）

::::tabs
tab: 演示效果

- 原价 :del[¥199]，现价 ¥99，限时特惠！
- 版本变更：:del[旧版 API] 已废弃，请使用新版接口
- 内容修订：:del[此处内容有误，已修正]

tab: 示例代码

`````markdown
- 原价 :del[¥199]，现价 ¥99，限时特惠！
- 版本变更：:del[旧版 API] 已废弃，请使用新版接口
- 内容修订：:del[此处内容有误，已修正]
`````

- 纯文本删除线效果，无需额外属性
- 常用于价格对比、版本变更、内容修订等场景

::::

---

## 上标/下标（sup/sub）

::::tabs
tab: 演示效果

- 化学式：H:sub[2]O 是水，CO:sub[2] 是二氧化碳
- 数学公式：x:sup[2] + y:sup[2] = z:sup[2]
- 带颜色的上标：E = mc:sup[2]{color="red"}
- 脚注引用：这是一段文字:sup[1]{color="blue"}
- 面积单位：房间面积为 50m:sup[2]

tab: 示例代码

`````markdown
- 化学式：H:sub[2]O 是水，CO:sub[2] 是二氧化碳
- 数学公式：x:sup[2] + y:sup[2] = z:sup[2]
- 带颜色的上标：E = mc:sup[2]{color="red"}
- 脚注引用：这是一段文字:sup[1]{color="blue"}
- 面积单位：房间面积为 50m:sup[2]
`````

- `:sup` 用于上标，`:sub` 用于下标
- `color` 可选值：`blue`、`green`、`red`、`yellow`、`purple` 或任意色值
- 常用于化学式、数学公式、脚注引用等场景

::::

---

## 键盘按键（kbd）

::::tabs
tab: 演示效果

- 复制操作：按 :kbd[Ctrl] + :kbd[C] 复制选中的内容
- 粘贴操作：按 :kbd[Ctrl] + :kbd[V] 粘贴内容
- 保存文件：按 :kbd[Ctrl] + :kbd[S] 保存当前文档
- Mac 快捷键：按 :kbd[⌘] + :kbd[K] 打开搜索
- 全屏截图：按 :kbd[PrtSc] 截取整个屏幕
- 撤销操作：按 :kbd[Ctrl] + :kbd[Z] 撤销上一步

tab: 示例代码

`````markdown
- 复制操作：按 :kbd[Ctrl] + :kbd[C] 复制选中的内容
- 粘贴操作：按 :kbd[Ctrl] + :kbd[V] 粘贴内容
- 保存文件：按 :kbd[Ctrl] + :kbd[S] 保存当前文档
- Mac 快捷键：按 :kbd[⌘] + :kbd[K] 打开搜索
- 全屏截图：按 :kbd[PrtSc] 截取整个屏幕
- 撤销操作：按 :kbd[Ctrl] + :kbd[Z] 撤销上一步
`````

- 模拟真实键盘按键外观
- 方括号内填写按键名称，支持任意文字
- 常用于快捷键说明、操作指南等场景

::::

---

## 模糊显示（blur）

::::tabs
tab: 演示效果

- 剧透警告：:blur[凶手其实是管家]
- 隐藏答案：这道题的答案是 :blur[42]
- 隐藏提示：:blur[点击此处查看隐藏提示]
- 谜语答案：什么东西早上四条腿，中午两条腿，晚上三条腿？答案是：:blur[人]

tab: 示例代码

`````markdown
- 剧透警告：:blur[凶手其实是管家]
- 隐藏答案：这道题的答案是 :blur[42]
- 隐藏提示：:blur[点击此处查看隐藏提示]
- 谜语答案：什么东西早上四条腿，中午两条腿，晚上三条腿？答案是：:blur[人]
`````

- 点击后移除模糊效果，显示隐藏内容
- 常用于剧透警告、答案隐藏、趣味互动等场景
- 无需额外属性

::::

---

## 密码遮罩（psw）

::::tabs
tab: 演示效果

- 数据库密码：数据库密码是 :psw[MySecretDB_2024]
- API 密钥：API 密钥为 :psw[sk-live-abc123xyz789]
- WiFi 密码：WiFi 密码：:psw[HelloWorld_5G]
- 服务器地址：服务器 IP：:psw[192.168.1.100]

tab: 示例代码

`````markdown
- 数据库密码：数据库密码是 :psw[MySecretDB_2024]
- API 密钥：API 密钥为 :psw[sk-live-abc123xyz789]
- WiFi 密码：WiFi 密码：:psw[HelloWorld_5G]
- 服务器地址：服务器 IP：:psw[192.168.1.100]
`````

- 点击后显示明文内容
- 默认显示为圆点遮罩，保护敏感信息
- 常用于展示密码、密钥、IP 地址等敏感信息

::::

---

## 复选框（checkbox）

::::tabs
tab: 演示效果

:checkbox[默认未选中]
:checkbox[已选中]{checked="true"}
:checkbox[绿色已选中]{checked="true" color="green"}
:checkbox[紫色加号]{checked="true" color="purple" symbol="plus"}
:checkbox[红色减号]{checked="true" color="red" symbol="minus"}
:checkbox[青色叉号]{checked="true" color="cyan" symbol="times"}

行内用法：今天需要 :checkbox[完成文档]{inline="true" checked="true"} 并 :checkbox[提交代码]{inline="true"}。

tab: 示例代码

`````markdown
:checkbox[默认未选中]
:checkbox[已选中]{checked="true"}
:checkbox[绿色已选中]{checked="true" color="green"}
:checkbox[紫色加号]{checked="true" color="purple" symbol="plus"}
:checkbox[红色减号]{checked="true" color="red" symbol="minus"}
:checkbox[青色叉号]{checked="true" color="cyan" symbol="times"}

行内用法：今天需要 :checkbox[完成文档]{inline="true" checked="true"} 并 :checkbox[提交代码]{inline="true"}。
`````

- 默认独占一行（块级），添加 `inline="true"` 可在段落中内联使用
- `checked="true"` 时显示选中态
- `symbol` 可选值：`plus`（加号）、`minus`（减号）、`times`（叉号），默认无符号
- `color` 支持 `blue`、`green`、`red`、`cyan`、`purple`、`orange` 等或任意色值

::::

---

## 单选框（radio）

::::tabs
tab: 演示效果

:radio[单选未选中]
:radio[单选已选中]{checked="true"}
:radio[单选橙色]{checked="true" color="orange"}
:radio[单选绿色]{checked="true" color="green"}

行内用法：请选择 :radio[选项 A]{inline="true" checked="true"} 或 :radio[选项 B]{inline="true"}。

tab: 示例代码

`````markdown
:radio[单选未选中]
:radio[单选已选中]{checked="true"}
:radio[单选橙色]{checked="true" color="orange"}
:radio[单选绿色]{checked="true" color="green"}

行内用法：请选择 :radio[选项 A]{inline="true" checked="true"} 或 :radio[选项 B]{inline="true"}。
`````

- 默认独占一行（块级），添加 `inline="true"` 可在段落中内联使用
- `checked="true"` 时显示选中态
- `color` 支持 `blue`、`green`、`red`、`cyan`、`purple`、`orange` 等或任意色值

::::

---

## 表情包（emoji）

在文章中插入表情包图片，支持多种表情源。行内指令，可直接在段落中使用。

::::tabs
 tab: 演示效果

 今天是开心的一天 :emoji[aini]{source="qq"}，代码终于跑通了！:emoji[OK]{source="qq"}

 Twemoji 风格的表情 :emoji[1f600]{source="twemoji"} :emoji[1f389]{source="twemoji"}

 贴吧表情 :emoji[huaji]{source="tieba"} :emoji[bishi]{source="tieba"}

 Blobcat 表情 :emoji[0_0]{source="blobcat"}

 也可以直接使用默认源（省略 source）：:emoji[aini]

 自定义高度：:emoji[party]{source="blobcat" height="3em"}

 tab: 示例代码

 `````markdown
 今天是开心的一天 :emoji[aini]{source="qq"}，代码终于跑通了！:emoji[OK]{source="qq"}

 Twemoji 风格的表情 :emoji[1f600]{source="twemoji"} :emoji[1f389]{source="twemoji"}

 贴吧表情 :emoji[huaji]{source="tieba"} :emoji[bishi]{source="tieba"}

 Blobcat 表情 :emoji[0_0]{source="blobcat"}

 也可以直接使用默认源（省略 source）：:emoji[aini]

 自定义高度：:emoji[party]{source="blobcat" height="3em"}
 `````

 - `source` 表情源，可选值：
   - `qq` — QQ 表情（GIF 格式）
   - `twemoji` — Twitter Emoji（SVG 格式）
   - `aru` — Aru 表情（GIF 格式）
   - `tieba` — 贴吧表情（PNG 格式）
   - `blobcat` — Blobcat 表情（GIF 格式）
   - `default` — 默认表情源（与 qq 相同）
   - 省略 `source` 时自动使用 `default` 源
 - `height` 自定义表情高度，默认 `1.75em`
 - 方括号内的内容为表情名称，会替换到 URL 中的 `{name}` 占位符

 ::::

---

## 步骤标记（step-brackets）

::::tabs
tab: 演示效果

:step-brackets[01]{title="克隆仓库"}

首先，从远程仓库克隆项目代码到本地。

:step-brackets[02]{title="安装依赖"}

进入项目目录，执行安装命令。

:step-brackets[03]{title="启动开发服务器"}

运行开发命令，在本地预览项目。

:step-brackets[04]{title="构建部署"}

执行构建命令，生成生产环境代码。

tab: 示例代码

`````markdown
:step-brackets[01]{title="克隆仓库"}

首先，从远程仓库克隆项目代码到本地。

:step-brackets[02]{title="安装依赖"}

进入项目目录，执行安装命令。

:step-brackets[03]{title="启动开发服务器"}

运行开发命令，在本地预览项目。

:step-brackets[04]{title="构建部署"}

执行构建命令，生成生产环境代码。
`````

- `title` 步骤标题，显示在步骤编号下方
- 方括号内填写步骤编号，支持任意文字（如 `01`、`1`、`Step 1` 等）
- 常用于教程步骤、操作指南、流程说明等场景

::::
